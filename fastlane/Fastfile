default_platform :ios

platform :ios do

PROJECT_NAME = "DevOpsTest";

  lane :beta do
    build_ios_app(
      clean: true,
      silent: true,
      # workspace: "project.xcworkspace",
      export_method: 'development'

      )
  end

end
